module std_base

// ======================================================
// Base Interfaces
// ======================================================

interface Equatable {
    fn equals(other: object) -> bool
    fn hashCode() -> int
}

interface Comparable {
    fn compareTo(other: object) -> int
}

interface Stringer {
    fn toString() -> string
}
